# 生产环境配置
app:
  name: "email-manage"
  port: 7900
  mode: "release"
  defaultRootPass: "sunny521"
  timezone: "Asia/Shanghai"

# 数据库配置 - 生产环境
database:
  driver: "mysql"
  host: "110.42.111.49"
  port: 1244
  username: "email"
  password: "tgiyGk32"
  name: "email"
  charset: "utf8mb4"

# JWT配置 - 一个月有效期
jwt:
  secret_key: "PD4GbH02PHF3YvRjonJf/2pgf5HGVwJOQguiYrRiZzdDIm/Bh0fH/8tmCkqr3krh"
  expires_in: 720                  # access token 30天（720小时）
  refresh_expires_in: 1440         # refresh token 60天（1440小时）
  issuer: "email-manage-app"
  audience: "email-manage-users"
  blacklist_enabled: true

# Redis配置 - 生产环境
redis:
  enabled: true
  host: "localhost"
  port: 6379
  password: ""
  db: 0

# 邮件服务配置 - 生产环境
mail:
  host: "smtpdm.aliyun.com"
  port: 465
  username: "nine_team@nineai.jiangly.com"
  password: "Nine98Mailer"
  from: "nine_team@nineai.jiangly.com"
  from_name: "系统通知"
  reply_to: "nine_team@nineai.jiangly.com"
  enabled: true

# CORS配置 - 生产环境
cors:
  enabled: true
  allowed_origins:
    - "https://email-manage.mmmss.com"
    - "http://email-manage.mmmss.com"
  allowed_methods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "OPTIONS"
  allowed_headers:
    - "Origin"
    - "Content-Type"
    - "Authorization"
  allow_credentials: true
  max_age: 86400

# 字段加密配置
security:
  encryption_key: "change-this-to-a-strong-random-key"

# 外部邮箱提供方配置
mail_provider:
  base_url: "https://app.wyx66.com"
